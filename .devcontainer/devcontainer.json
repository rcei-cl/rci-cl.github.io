// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/jekyll
{
	"name": "Jekyll",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	"image": "mcr.microsoft.com/devcontainers/jekyll:2-bookworm",

	// Features to add to the dev container. More info: https://containers.dev/features.
	// "features": {},

	// Puerto del sitio (4000) y de livereload (35729).
	"forwardPorts": [4000, 35729],
	"portsAttributes": {
		"4000": {
			"label": "Jekyll",
			"onAutoForward": "openPreview"
		},
		"35729": {
			"label": "LiveReload",
			"onAutoForward": "silent"
		}
	},

	// Instala las gemas al crear el contenedor.
	"postCreateCommand": "bundle install --gemfile=docs/Gemfile",

	// En cada inicio: actualiza RubyGems/Bundler/gemas del sitio y levanta
	// Jekyll con autoreload en segundo plano.
	"postStartCommand": "bash .devcontainer/post-start.sh"

	// Configure tool-specific properties.
	// "customizations": {},

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"
}
